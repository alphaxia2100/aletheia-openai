# Final v2 independent coverage verification

Verifier: fresh-context independent scope auditor (`/root/final_scope_auditor_v2`)  
Run: `2026-07-27-101249-aletheia-output-quality-20260727`  
Date: 2026-07-27

## Scope and method

I read the complete current 440-line `brief.md`, all 89 current `claims.jsonl` rows, all 89 lexical `verify.jsonl` rows, the complete internal operational-observations record, the current local experiment records, and the cited/full source captures needed for the reported literature claims. Lexical relevance was not treated as semantic support. I did not run `report.py audit-claims` and did not alter canonical artifacts.

The verdict proposal preserves each current claim text exactly and selects one URL declared by that row, whether the source is stored in `url` or the multi-source `urls` array. Lexical relevance was not treated as proof.

## Verdict result

| Scope | Supported | Contradicted | Unsupported | Total |
|---|---:|---:|---:|---:|
| Current ledger claims | 89 | 0 | 0 | 89 |
| Omitted proposed additions | 6 | 0 | 4 | 10 |
| Reconciled proposal | 95 | 0 | 4 | 99 |

All current rows declare a source, including six multi-source rows whose sources are in `urls`; those were re-read and assigned one declared URL in the verdict proposal. Their bounded synthesis language is supported as written, not as universal law.

## Coverage findings

The revised opening correctly labels the seven-plane order and portfolio retention as provisional project policy, not comparative findings. The tool-failure/empty-index rule and final branch-release requirement are likewise explicitly labeled project policies. Their factual premises still require support; the observed read, routing, channel, scope, and cost defects are adequately ledgered.

The dossier exact byte claim, 164/164 versus 156/156 test claim, claim/question graph proposal labels, HiAgent scope, branch-table ledger/observability states, and final branch-release policy are now in the ledger and supported as stated. The brief also correctly qualifies cross-study disagreement and absence claims in prose; however, the corresponding ledger rows still need declared sources.

Ten omitted load-bearing statements remain in `final-v2-additions-proposal.jsonl`. Four require revision or dedicated evidence before they can be attested: the directory-tree comparative judgment, the combined MindSearch/Co-STORM improvement-and-limit characterization, the universal-looking hierarchy condition, and the unsupported conditional cross-study interpretation. The remaining additions are implementation, project-state, sampling, or properly labeled policy/proposal claims.

## Required changes before a final attestation

1. Add the ten proposed omissions or explicitly narrow/remove them; retain authorial policy labels for sections 11–12 and the final recommendation.
2. Narrow the directory-tree and hierarchy claims to unvalidated project judgment unless a direct comparative evaluation is supplied.
3. Cite or remove the broad combined MindSearch/Co-STORM characterization rather than inferring it from question-graph lexical overlap.
4. Re-run a fresh independent whole-brief coverage pass after any change; the current artifacts cannot be attested while omitted load-bearing scope remains.

## Validation

`final-v2-verdicts-proposal.jsonl` has 89 JSON rows, one for every current exact ledger claim. `final-v2-additions-proposal.jsonl` has 10 JSON rows. Both files are JSONL proposals only.
